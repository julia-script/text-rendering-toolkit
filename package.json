{
  "name": "webgpu-text",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "biome check . && turbo run typecheck test",
    "check:write": "biome check --write .",
    "docs:build": "turbo run build --filter=@webgpu-text/docs",
    "docs:dev": "turbo run build --filter=@webgpu-text/docs^... && pnpm --filter @webgpu-text/docs dev",
    "format": "biome format --write .",
    "release:candidate": "pnpm build && vitest run release-candidate/validate.test.ts",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "turbo": "2.10.5",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
